version: '3.8'
services:
  local-ai-worker:
    image: localai/localai:latest-aio-cpu
    command: --models-path /models 
     --context-size 2048  --threads 19 --localai-config-dir /configuration worker p2p-llama-cpp-rpc --token ${P2PTOKEN}
    environment:
      - DEBUG=true
    volumes:
      - ./models:/models
      - ./tmp/generated/images:/tmp/generated/images
      - ./configuration:/configuration
    ports:
      - 8090:8090


    restart: unless-stopped


